// template literal allows us use backtiks to make shorter codes
let firstName = `Chidera`
let lastName = `Umejuru`
const fullName = `${firstName} ${lastName}`
console.log(fullName);

// makes multi-line easier
let myName = `Chidera Umejuru`
let age = 20
let gender = `female`
const info = `${myName}
${age}
${gender}
`
console.log(info);